import { CustomColumn } from "../atoms/CustomColumn";
import CardTitle from "../molecules/CardTitle";

export default function CustomBody() {
    return (
        <CustomColumn>
            <CardTitle/>
        </CustomColumn>
    );
}